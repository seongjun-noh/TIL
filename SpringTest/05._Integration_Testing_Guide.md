# 통합 테스트 가이드

## 관련 가이드
- [스프링 부트 테스트 가이드](02.%20Spring%20Boot%20Test%20Guide.md) - 전체 테스트 가이드
- [JUnit 5 가이드](03.%20JUnit%205%20Guide.md) - 테스트 프레임워크 기본
- [Mock 테스트 가이드](04.%20Mock%20Testing%20Guide.md) - Mock 객체를 활용한 테스트

## 통합 테스트 기초

### 통합 테스트의 특징
- 실제 데이터베이스와의 상호작용 테스트
- 실제 쿼리 실행과 결과 확인
- DB 관련 설정이나 성능 이슈 발견
- 여러 컴포넌트 간의 상호작용 검증

### 테스트 데이터베이스 설정
```yaml
# application-test.yml
spring:
  jpa:
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
```

## Repository 통합 테스트

### 기본 Repository 테스트
```java
@DataJpaTest
class UserRepositoryTest {
    @Autowired
    private UserRepository userRepository;
    
    @Test
    void saveUser_ValidInput_ReturnsSavedUser() {
        // given
        User user = new User("test@test.com", "password");
        
        // when
        User savedUser = userRepository.save(user);
        
        // then
        assertNotNull(savedUser.getId());
        assertEquals("test@test.com", savedUser.getEmail());
    }
}
```

### 쿼리 메서드 테스트
```java
@DataJpaTest
class UserRepositoryTest {
    @Autowired
    private UserRepository userRepository;
    
    @Test
    void findByEmail_ExistingUser_ReturnsUser() {
        // given
        User user = new User("test@test.com", "password");
        userRepository.save(user);
        
        // when
        Optional<User> found = userRepository.findByEmail("test@test.com");
        
        // then
        assertTrue(found.isPresent());
        assertEquals("test@test.com", found.get().getEmail());
    }
}
```

## REST API 통합 테스트

### 컨트롤러 테스트
```java
@SpringBootTest
@AutoConfigureMockMvc
class UserControllerTest {
    @Autowired
    private MockMvc mockMvc;
    
    @Autowired
    private UserRepository userRepository;
    
    @Test
    void createUser_ValidInput_Returns201() throws Exception {
        // given
        UserDto userDto = new UserDto("test@test.com", "password");
        
        // when & then
        mockMvc.perform(post("/api/users")
                .contentType(MediaType.APPLICATION_JSON)
                .content(objectMapper.writeValueAsString(userDto)))
                .andExpect(status().isCreated())
                .andExpect(jsonPath("$.email").value("test@test.com"));
    }
}
```

### 페이징 API 테스트
```java
@SpringBootTest
@AutoConfigureMockMvc
class ProductApiTest {
    @Autowired
    private MockMvc mockMvc;
    
    @Autowired
    private ProductRepository productRepository;
    
    @Test
    void getProducts_WithPagination_ReturnsProductList() throws Exception {
        // given
        List<Product> products = Arrays.asList(
            new Product("상품1", 1000),
            new Product("상품2", 2000)
        );
        productRepository.saveAll(products);
        
        // when & then
        mockMvc.perform(get("/api/products")
                .param("page", "0")
                .param("size", "10")
                .param("sort", "price,desc"))
                .andExpect(status().isOk())
                .andExpect(jsonPath("$.content", hasSize(2)))
                .andExpect(jsonPath("$.content[0].name").value("상품2"))
                .andDo(print());
    }
}
```

## 테스트 데이터 관리

### 테스트 데이터 준비
```java
@TestConfiguration
public class TestConfig {
    @Bean
    public DataLoader dataLoader() {
        return new DataLoader() {
            @Override
            public void loadData() {
                // 테스트용 초기 데이터 설정
            }
        };
    }
}
```

### SQL 스크립트 실행
```java
@Test
@Sql({"/schema.sql", "/data.sql"})
void testWithSqlScripts() {
    // SQL 스크립트 실행 후 테스트
}
```

## 비동기 작업 테스트

### 비동기 서비스 테스트
```java
@SpringBootTest
class AsyncServiceTest {
    @Autowired
    private AsyncService asyncService;
    
    @Test
    void processAsync_LongRunningTask_CompletesSuccessfully() {
        // given
        CompletableFuture<String> future = asyncService.processAsync();
        
        // when & then
        assertTimeout(Duration.ofSeconds(5), () -> {
            String result = future.get();
            assertEquals("작업완료", result);
        });
    }
}
```

## 성능 테스트

### 벌크 작업 성능 테스트
```java
@Test
void bulkOperation_WithLargeDataSet_CompletesWithinTimeLimit() {
    // given
    List<Item> items = generateLargeItemList(1000);
    
    // when
    long startTime = System.currentTimeMillis();
    itemService.bulkSave(items);
    long endTime = System.currentTimeMillis();
    
    // then
    assertTrue((endTime - startTime) < 5000, "벌크 작업이 5초 이내에 완료되어야 함");
}
```

## 테스트 프로파일 활용

### 프로파일 설정
```java
@ActiveProfiles("test")
@SpringBootTest
class ServiceTest {
    // 테스트 프로파일을 사용한 테스트 코드
}
```

### 테스트 유틸리티
```java
@UtilityClass
public class TestUtils {
    public static String createRandomEmail() {
        return UUID.randomUUID().toString() + "@test.com";
    }
    
    public static UserDto createTestUser() {
        return UserDto.builder()
                .email(createRandomEmail())
                .name("테스트 사용자")
                .build();
    }
}
```

## 모범 사례

### 테스트 격리
- 각 테스트는 독립적으로 실행 가능해야 함
- `@Transactional` 활용
- 테스트 데이터 정리

### 테스트 실행 최적화
- 필요한 빈만 로드
- 적절한 테스트 프로파일 사용
- 테스트 데이터베이스 최적화

## 다음 단계
1. [스프링 부트 테스트 가이드](02.%20Spring%20Boot%20Test%20Guide.md) - 전체 테스트 가이드로 돌아가기

## 참고 자료
- [스프링 부트 공식 문서](https://docs.spring.io/spring-boot/docs/current/reference/html/spring-boot-features.html#boot-features-testing) 